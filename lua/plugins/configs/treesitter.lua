local options = {
  ensure_installed = { "lua", "javascript", "typescript", "python", "tsx" },

  highlight = {
    enable = true,
    use_languagetree = true,
  },

  indent = { enable = false},
}

return options
